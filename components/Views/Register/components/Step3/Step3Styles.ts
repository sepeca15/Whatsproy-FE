import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    flex: 1,
    display: "flex",
    flexDirection: "column",
  },
  errorText: {
    color: "red",
    fontSize: 12,
    marginTop: 4,
  },
  textCenterLg: {
    marginBottom: 20,
    textAlign: "center",
    fontSize: 18,
  },
});
